[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nube-eficiente"
version = "0.1.0"
description = "Plataforma SaaS de FinOps inteligente para Azure"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = [".."]
include = ["app*"]